class Vehicle {
    
     constructor(model, brand, year, plate, color, chassi, date, value, employ, bonus){
         this.model = model;
         this.brand = brand;
         this.year = year;
         this.plate = plate;
         this.color = color;
         this.chassi = chassi;
         this.date = date;
         this.value = value;
         this.employ = employ;
         this.bonus = bonus;
     }
    
}